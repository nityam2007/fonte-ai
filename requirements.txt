# FONTe AI - Dependencies
# Minimal dependencies - uses Python stdlib where possible

# Required
fonttools>=4.0.0      # Font parsing and glyph extraction
torch>=2.0.0          # PyTorch for model training

# Note: All other functionality uses Python standard library:
# - xml.etree.ElementTree for SVG parsing
# - re for regex (pre-compiled patterns)
# - concurrent.futures for parallel processing
# - json, pathlib, argparse, dataclasses, typing, etc.
